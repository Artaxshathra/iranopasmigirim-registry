{
  "name": "shahin-mirror",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub-only request-driven offline mirror extension and producer pipeline.",
  "scripts": {
    "build": "node scripts/release-gate.js && node build.js both",
    "build:chrome": "node scripts/release-gate.js && node build.js chrome",
    "build:firefox": "node scripts/release-gate.js && node build.js firefox",
    "dev:chrome": "node build.js chrome",
    "dev:firefox": "node build.js firefox",
    "dev:both": "node build.js both",
    "test:local:chrome": "npm test && npm run dev:chrome",
    "test:local:firefox": "npm test && npm run dev:firefox",
    "test": "node --test test/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "openpgp": "^6.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
